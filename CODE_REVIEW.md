# 코드 리뷰 메모 — 하이라이트 프로토타입

> 작성: 2026-06-08 / 대상 브랜치: `claude/code-review-7dufx`
> 용도: 집에서 코드 고칠 때 참조용 체크리스트. 수업 MVP 기준이라 "서비스 완성도"가 아니라
> **데모에서 안 깨지고 흐름이 자연스러운가**에 초점을 둠.

## ⚠️ 먼저 알아둘 것 (범위)

- 이 문서는 **이 브랜치(`code-review-7dufx`)의 옛 프로토타입** 기준이다.
  하이라이트 드래그 / 동그라미 제스처 / 밑줄·주석 / 신호 대시보드까지만 있는 버전.
- 집 세션이 작업한 **`claude/viewer-layout` 브랜치는 이보다 훨씬 앞서 있다** (확장 뷰어,
  촛불 개입, 마찰 계수, board 모드, AI 티키타카, 멀티세션 리포트 등 97파일·25k줄).
  → **실제 발표/데모 코드가 viewer-layout이면, 그 브랜치를 따로 리뷰해야 한다.**
  아래 항목 중 일부는 viewer-layout에서 이미 해결됐을 수 있음.

---

## A. 고치면 좋은 것 (MVP 데모에 직접 영향)

### A-1. 대시보드가 마우스 움직일 때마다 통째로 다시 그려짐 ★우선
- `dashboard.js` `onSignal()` 끝에서 시그널 종류와 무관하게 항상 `paint()` 호출 (95행).
- `mouse_trail`은 120ms마다(`signals.js:115`), `scroll`도 계속 발생 → 마우스를 움직이는
  내내 초당 8회 이상 `innerHTML` 3블록(reading/highlight/recent)을 통째로 재생성.
- 데모 중 패널이 미세하게 깜빡이거나 버벅일 수 있음.
- **고치기:** `switch`에서 통계가 실제로 바뀐 case일 때만 `paint()` 호출. 타임라인 칩은
  지금처럼 따로 append.

### A-2. transitioning 상태에서 빠져나올 출구가 약함 (드물게 잠김)
- 드래그가 문단 밖으로 나가면 `transitioning` 진입. 여기서 벗어나는 길은
  *마우스가 멈춰 idle 타이머가 발화*하는 것뿐 (`highlight.js:206~`).
- 이 단계엔 Esc 핸들러도, mouseup 처리도 없음(`onMouseUp`은 `drawing`만, 220행).
- `onMouseDown`은 `state !== "idle"`이면 무시(111행) → 세션이 떠 있는 동안 새 하이라이트
  시작이 막힘. window blur로만 복구.
- **고치기:** `transitioning`에서도 Esc / 우클릭 / 빈 곳 클릭으로 `cleanupSession()` 부르는
  탈출구 추가.

### A-3. 외부 의존성(esm.sh, Google Fonts)에 통째로 매여 있음
- `pretext_helpers.js:13`이 모듈 최상단에서 `https://esm.sh/@chenglou/pretext` import.
  네트워크 차단/오프라인/esm.sh 장애 시 이걸 import하는 `highlight.js`·`reader.js`까지
  전부 죽어 화면이 백지가 됨.
- **발표장 와이파이가 막히면 데모가 통째로 안 뜰 수 있는 리스크.**
- **고치기:** pretext를 로컬에 동봉(vendoring)해서 import. 폰트도 로컬 fallback 지정.
  (viewer-layout엔 이미 `extension/viewer/vendor/pretext/`로 vendoring돼 있음 — 거기 방식 참고.)

### A-4. 미사용 코드
- `pretext_helpers.js:91` `getWordRangeAt` — export만 되고 아무 데서도 안 씀(grep 확인). 단어
  단위 선택 로직 통째로 dead code. 삭제 or 연결.

---

## B. 기획서 기준 UX / 흐름 점검

> 기획서 핵심(changelog/Private 문서 기준): *다회독 마찰 감소 + 메타인지 리포트*,
> *Seam 타겟팅 능동 개입*, *"독자는 시스템의 능동적 개입을 환영한다"* 페르소나.

### B-1. 주석을 열려면 "문단 밖으로 드래그 → 멈춤"이 필요한데 발견이 어려움
- 밑줄을 긋다 문단 경계를 넘겨야 동그라미+잉크가 나오고, 거기서 600ms 멈춰야 주석창이 뜸.
- 이 제스처를 **알려주는 단서(affordance/힌트)가 전혀 없음.** 처음 보는 사람은 절대 모름.
- 능동 개입을 환영하는 페르소나라 해도, "어떻게 개입하는지"는 가르쳐줘야 함.
- **제안:** 최초 1회 온보딩 힌트, 또는 밑줄 끝에 "→ 끌어서 메모" 같은 미세 가이드.

### B-2. 600ms 멈춤 → 주석창 자동 등장이 오발될 수 있음
- 문단 밖으로 마우스를 빼다 잠깐 멈추기만 해도 주석창이 뜸. "갑자기 텍스트박스가 튀어나온다"는
  느낌을 줄 수 있음.
- **제안:** 임계 시간을 약간 늘리거나, 잉크가 일정 길이 이상 뻗었을 때만 주석 진입 허용.

### B-3. 선택/표시 메커니즘이 3종이라 시각 언어가 섞임
- 드래그 밑줄(빨강 희미한 줄) · 동그라미 제스처(회색 타원) · 주석(빨간 점 마커) — 세 가지가
  각기 다른 모양/색으로 남음.
- 기획서가 "마찰 감소"를 지향하는데, 표식 체계가 셋이면 오히려 인지 부하가 될 수 있음.
- **점검:** 각 표식의 "의미"가 사용자에게 구분되어 전달되는가? 색/모양 위계를 한 번 정리.

### B-4. 대시보드가 "신호 나열"이지 "메타인지 리포트"가 아님
- 현재 대시보드는 dwell/reread/밑줄/주석 **카운트**와 타임라인 칩만 보여줌.
- 기획서의 핵심 산출물은 **마찰 계수 + ICAP/load 해석 = 메타인지 리포트**.
  이 프로토타입 대시보드는 아직 거기까지 못 감. (← viewer-layout이 바로 이걸 채웠음.)
- **점검:** 데모에서 보여줄 "리포트"가 이 카운터 패널이면 기획 의도와 갭이 큼.

### B-5. 마우스 트레일(잉크 자국)이 상시 켜져 있음
- 커서를 따라 잉크가 계속 흐름. 예쁘지만 "조용히 깊게 읽기"엔 시각 노이즈일 수 있음.
- **점검:** 읽기 몰입 vs 인터랙션 피드백 사이 균형. 옵션화하거나 강도를 낮춰도 됨.

### B-6. 촛불(능동 개입)이 이 프로토타입엔 아예 없음
- 기획서 중심 장치인 촛불/Seam 개입이 이 브랜치엔 없음. 즉 이 코드만으로는 "능동적 개입"
  컨셉이 화면에 안 드러남. (viewer-layout엔 구현돼 있음.)
- **결론:** 발표 데모는 viewer-layout 기준으로 가는 게 맞아 보임.

---

## C. 시간 남으면 정리 (성능·유지보수)

- **C-1.** `applyUnderline`이 매 이동마다 문단 전체 span 순회(`highlight.js:234`) →
  변경된 lo/hi 구간만 토글하면 됨.
- **C-2.** `initGestureDetector`가 매 이동마다 전 포인트 정렬+표준편차(`signals.js:336~`). 무거움. 쓰로틀 가능.
- **C-3.** `readSelectedText`/`freezeUnderline`/`placeAnnotationMarker`가 char마다 `querySelector`
  루프. 한 번의 `querySelectorAll`+인덱싱으로 대체.
- **C-4.** `console.log("[signal]", ...)` 상시 출력(`signals.js:27`) → 디버그 플래그로 감싸기.
- **C-5.** `cursor-hud.js`는 튜닝용 임시 패널(본인 주석). 값 확정됐으면 `app.js:8` 호출 +
  파일 + `styles.css` HUD 블록 같이 삭제. (현재 `styles.css:81` 하드코딩 커서값과
  HUD 기본값이 따로 관리돼 어긋날 위험.)
- **C-6.** 창 리사이즈/폰트 변경 시 pretext 캐시·`word-circle-mark` 위치 안 맞음
  (`pretext_helpers.js:151` TODO). 데모를 한 화면 크기로만 보여줄 거면 무시 가능.
- **C-7.** `charIndexToCursor`가 줄 길이를 UTF-16 코드유닛으로 셈(`pretext_helpers.js:54`).
  한글+ASCII 본문에선 정확하지만 이모지/결합문자 들어오면 인덱스 어긋남.

---

## D. 안 봐도 되는 것 (의도된 사양 — 확인됨)

- **본문 텍스트 선택 불가(`user-select:none`)** — 드래그 피처를 위해 일부러 뺀 것. 정상.
- **키보드만으로 주석 작성 불가** — 필요 없음(마우스 인터랙션 전제). 정상.
- **XSS** — 주석 입력은 `textContent`/`escapeHtml`로 처리돼 안전(`dashboard.js:194,210`). 문제 없음.

---

## 부록. 집 세션(`claude/viewer-layout`) 현황 메모

- 오늘 커밋: 13:25 (티키타카 opener) → 13:38 (그림자 폴리시) → **15:19 (changelog 정리)**.
  마지막이 changelog라 **한 작업 단락을 마무리한 상태**로 보임.
- main 대비 **97파일 / +25,336줄**. 주요 빌드물:
  - 확장 뷰어(`extension/viewer/`, pretext vendoring 포함)
  - 신호 파이프라인 5단계(dwell 보강 → 촛불 Seam → 마찰 계수 → 대시보드 → board 모드)
  - AI 티키타카(촛불 → 멀티턴 채팅), interpret.js / scripts/interpret.py
  - 멀티세션 거시 리포트(localStorage), 보드 화이트보드 UI, 레이어드 그림자
- 자세한 내역은 그 브랜치의 `CHANGELOG.md` 참고.
- **다음 액션 제안:** 발표 코드가 viewer-layout이면, 이 문서 말고 **viewer-layout을 새로
  리뷰**하는 게 맞음. 원하면 거기 기준으로 다시 정리해줄 수 있음.
